import React from "react";
import { Col } from "react-bootstrap";

const UserElements = props => {
  return (
    <Col xs={12} xl={9}>
      UserElements
    </Col>
  );
};

export default UserElements;
